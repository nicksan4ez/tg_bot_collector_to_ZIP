# Example .env configuration
# Copy this file to .env and fill in your real values.

BOT_TOKEN=your_bot_token_here
# Comma-separated list of Telegram user IDs allowed to use the bot. Leave blank to allow everyone.
ALLOWED_USERS=

# Where media will be stored temporarily.
TMP_ROOT=./telegram_bot_media

# Name of the archive that will be sent back to the user.
ZIP_NAME=Архив.zip

# Delay (in seconds) before the bot bundles received files into an archive.
ARCHIVE_DELAY=5

# Maximum size (MB) of a single archive part; larger archives are split.
ARCHIVE_SIZE_LIMIT_MB=48
